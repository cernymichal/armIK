using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
public class IKRotaryJoint : IK1DOFJoint {
    [SerializeField, Range(0, 360)] public float maxAngle = 360.0f;

    private void OnDrawGizmos() {
        DrawLineToChild();
        Gizmos.color = Color.red;
        Utils.GizmosDrawArc(transform.localToWorldMatrix, Vector3.forward, Vector3.up, maxAngle);
    }
}
*/
